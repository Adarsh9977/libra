# OpenAI (required for agent and embeddings)
OPENAI_API_KEY=

# Google Drive OAuth (required for Drive search and ingestion)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
# Optional: default is NEXTAUTH_URL + /api/drive/oauth/callback
# GOOGLE_REDIRECT_URI=http://localhost:3000/api/drive/oauth/callback

# App URL (used for OAuth redirect base)
NEXTAUTH_URL=http://localhost:3000

# Postgres with pgvector (required for vector search and Drive tokens)
DATABASE_URL=postgresql://user:password@localhost:5432/libra

# Serper (required for web search tool)
SERPER_API_KEY=
